{% if content %}
  <section{{ attributes.addClass('region', 'region--content-top') }}>
    <div class="site-container">
      <div class="region--content-top__inner">
        {{ content }}
      </div>
    </div>
  </section>
{% endif %}
