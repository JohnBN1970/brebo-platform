# BREBO theme-logo

Het standaardlogo van `brebo_theme` staat als `logo.svg` in deze theme-root en wordt via Git gedeployed.

Laat Drupal voor dit theme het standaardlogo gebruiken. Een runtime-only `public://`-logo mag deze versie niet vervangen zonder dat het bronbestand ook in de repository wordt bijgewerkt.
